# Business Grafts

Portable business logic packages that can be grafted into `src/features/`.

A graft is more than tables or pages. It should carry its manifest, invariants,
schemas, capabilities, role templates, navigation, routes, workflows, statuses,
tests, and integration notes.
