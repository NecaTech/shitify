# Compositions

Reusable assemblies of catalog invariants and business grafts.

Compositions help validate a project shape during development. They must be
materialized into `src/` before a client project reaches staging.
